# 1. 프로젝트 개요

- **프로젝트명:** 결타래 (Gyeoltarae, 예전 이름 WikiDesk)
- **프로젝트 목적:** 나무위키 스타일 마크업으로 세계관·설정·메모를 정리하는 완전 오프라인 로컬 위키 데스크톱 앱 구축. 서버나 로그인 없이, 아래아한글처럼 로컬 .md 파일에 직접 저장.
- **개발 환경:** Claude Code (CLI 기반 AI 페어 프로그래밍) + Vite
- **아키텍처:** Electron 메인 프로세스(파일시스템·워크스페이스 관리) + React 렌더러(SPA), 서버·DB 서버 없이 단일 실행 파일로 배포

# 2. 기술 스택 (Tech Stack)

| 분류 | 기술 및 라이브러리 | 용도 및 특징 |
|---|---|---|
| Core | Electron (v44), React (v19), Vite | 데스크톱 런타임, UI 라이브러리, 초고속 프론트엔드 빌드 툴 |
| State | Zustand | 워크스페이스·문서·모드·테마 상태를 단일 스토어로 관리 |
| File System | Node fs, chokidar | 워크스페이스 폴더 실시간 감시 → 외부 편집 시 트리 자동 반영 |
| Icons | Lucide React | 서식 툴바·모달 전반의 아이콘 시스템 |
| Packaging | @electron/packager | electron-builder 환경 이슈를 우회한 대체 패키징 경로 |
| Metadata | JSON 파일 인덱스 | 태그·제목 메타데이터, 추후 SQLite 무중단 교체 가능하도록 로직 분리 |

# 3. 핵심 기능 (Core Features)

## 3.1 나무위키 스타일 마크업 파서 (자체 구현, 외부 라이브러리 無)

- 실제 나무위키 문법을 그대로 구현: 제목(`=`~`======`), 굵게/기울임(`'''`/`''`), 위·아래첨자, 중첩 목록/인용, 각주 자동 번호, 표 셀 확장 속성(병합·정렬·배경색), 표 전체 속성(`tablebordercolor`, `rowbgcolor`, `colbgcolor` 등)
- 표 셀 안에 여러 줄로 이어지는 `{{{#!wiki style="..."}}}` 블록을 중첩 파싱 — 실제 나무위키의 그라데이션 셀 구조를 그대로 재현
- CSS 속성 화이트리스트 기반 새니타이저로 그라데이션·그림자·테두리 등 자유 스타일을 안전하게 허용하면서 `position`/`url()`/외부 리소스는 원천 차단
- 결타래 전용 확장 문법(`#` 제목, `**굵게**` 등)과 실제 나무위키 문법을 표로 분리 문서화해 문서 이식성 보장

## 3.2 뷰어 · 듀얼 · 편집 3단 모드

- 헤더 정중앙 모드 스위처, 듀얼 모드는 좌측 실시간 뷰어 · 우측 원문 에디터
- 제목 자동 번호 매기기(1. / 1.1. / 2.), 접고 펼 수 있는 목차(`[목차]`)
- 뷰어 우측에 항상 표시되는 플로팅 목차 — 호버 시 펼쳐지고 클릭하면 애니메이션 없이 즉시 해당 위치로 이동

## 3.3 서식 툴바 & 실행취소 안전성

- 아이콘 기반 그룹 툴바(구조/서식/삽입)로 문법을 몰라도 버튼만으로 서식 삽입
- 모든 삽입을 `document.execCommand('insertText')` 경유로 처리해 네이티브 Ctrl+Z 실행취소 스택과 완전히 호환되도록 구현 (기존 직접 대입 방식은 되돌리기가 끊기는 버그가 있어 발견 즉시 수정)

## 3.4 표 편집기 (스프레드시트급 편집)

- Shift+클릭으로 사각 범위 선택 → 배경색·글자색·정렬을 선택 범위 전체에 일괄 적용
- Ctrl+C/V로 셀 범위를 서식 포함 복사·붙여넣기, 대상이 그리드보다 크면 행/열 자동 확장, 엑셀 등 외부 표의 탭 구분 텍스트 붙여넣기도 지원
- 재사용 가능한 파스텔 팔레트 색상 선택기(ColorPicker) 컴포넌트 자체 제작 — 표준색 11개 + 톤 4단계 팔레트 + 직접 선택

## 3.5 그라데이션 편집기

- 텍스트/표 셀 두 모드, 8방향 + 원형, 색상 2~4개, 실시간 미리보기 후 진짜 나무위키 문법으로 삽입
- 처음엔 정적 예시 텍스트만 삽입하던 것을 표 편집기와 동일한 수준의 실시간 편집 모달로 재작업

## 3.6 파일시스템 & 워크스페이스

- 워크스페이스 폴더 실시간 감시(chokidar) → 외부에서 파일이 바뀌면 트리 자동 갱신
- 사이드바에 `.md`/`.txt` 드래그 앤 드롭으로 새 문서 즉시 불러오기
- 800ms 디바운스 자동저장 및 저장 상태 표시(저장 중/저장됨/저장 실패)
- 삭제는 휴지통 이관 방식으로만 동작(영구 삭제 없음)

## 3.7 Electron 메뉴 & 테마

- 기본 메뉴(파일/편집/보기/창) 전체 한글화
- 시스템 / 라이트 / 다크 3단 테마 토글, localStorage에 기억

# 4. 시스템 구조 및 설계 사상

- `electron/`: 메인 프로세스 — 파일 I/O, 워크스페이스 스캔, chokidar 감시, JSON 기반 메타데이터 인덱스
- `src/lib/wikiParser.js`: 외부 마크다운 라이브러리 없이 순수 함수로 구현한 나무위키 문법 파서
- `src/components/`: 표 편집기·그라데이션 편집기·색상 선택기 등 재사용 가능한 모달형 유틸리티로 분리
- `src/store/`: zustand 단일 스토어로 워크스페이스/문서/모드/테마 상태 관리
- 메타데이터 인덱스는 네이티브 빌드 도구 없이도 동작하도록 우선 JSON 파일로 구현, 추후 better-sqlite3 등으로 교체 가능하게 로직만 분리

# 5. 배포 엔지니어링 — Windows 패키징 트러블슈팅

- **배경:** electron-builder로 nsis 설치파일을 빌드하려 했으나, 이 개발 환경에서 `EPERM: rename win-unpacked.tmp` 오류가 재현됨.
- **진단:** 관리자 권한 재실행, Windows Defender 예외 등록, 제어된 폴더 액세스 해제로도 해결되지 않았고, electron-builder를 거치지 않은 순수 Node.js `fs.rename` 호출로도 동일하게 재현되어 electron-builder만의 버그가 아님을 직접 격리·확인.
- **해결:** `@electron/packager` 기반 대체 빌드 경로를 구축 — 파일을 개별 복사해 디렉터리 전체를 통째로 rename하지 않는 방식이라 동일 문제를 우회. 빌드 완료 후 프로젝트 루트에 실행 바로가기(.lnk)를 자동 생성하는 스크립트까지 추가해, 중첩된 빌드 출력 폴더까지 매번 들어가지 않아도 되도록 UX 보완.
- 두 빌드 경로(electron-builder / electron-packager)를 모두 남겨 환경에 따라 선택할 수 있도록 구성.

# 6. 개발 방법론

- 전 과정 Claude Code와의 대화형 페어 프로그래밍으로 진행. 요구사항을 하나씩 던지면 실제 나무위키 문법 도움말을 직접 리서치(WebFetch로 공식 문서 확인)한 뒤 구현하는 방식으로, 추측이 아닌 검증된 문법만 반영.
- 기능을 만들 때마다 브라우저 프리뷰에서 실제 클릭·타이핑·드래그·복사붙여넣기로 동작을 검증한 뒤 exe를 재빌드하는 사이클을 반복.
- 검증 과정에서 발견한 실제 버그(실행취소 끊김, 글자색 `#` 중복 등)를 바로 재현·수정하고 기록.
